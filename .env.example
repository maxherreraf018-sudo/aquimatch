# Copia este archivo como ".env" (sin el ".example") y completa el valor real.
# NUNCA subas el archivo ".env" a un repositorio público (contiene tu clave de Google).

VITE_GOOGLE_PLACES_API_KEY=TU_API_KEY_DE_GOOGLE_PLACES
